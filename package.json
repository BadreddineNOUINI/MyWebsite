{
  "name": "netlify-p1",
  "private": true,
  "version": "1.0.0",
  "description": "Minimal Netlify frontend + function with blobs storage",
  "dependencies": {
    "@netlify/blobs": "^10.0.8"
  }
}
